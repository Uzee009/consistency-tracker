# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
Error SS/

.gemini/
gha-creds-*.json

# Throwaway cross-device sync experiment (PocketBase binary, pb_data, Dart spike client)
sync_spike/

# Claude Code per-machine settings (local permission grants — not shared)
.claude/settings.local.json

# PocketBase runtime (binary + data are local-only; migrations are tracked)
pocketbase/*
!pocketbase/pb_migrations/
!pocketbase/pb_migrations/**

# Local dev config — contains credentials. Use config/dev.json.example as a template.
config/dev.json

